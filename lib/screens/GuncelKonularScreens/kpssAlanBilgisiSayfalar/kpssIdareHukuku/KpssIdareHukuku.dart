import 'package:flutter/material.dart';

class KpssIdareHukukuPage extends StatelessWidget {
  const KpssIdareHukukuPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('İdare Hukuku')),
      body:
          const Center(child: Text('İdare Hukuku içeriği burada yer alacak.')),
    );
  }
}
