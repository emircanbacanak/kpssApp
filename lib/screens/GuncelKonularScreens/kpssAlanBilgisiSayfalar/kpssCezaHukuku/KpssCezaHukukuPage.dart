import 'package:flutter/material.dart';

class KpssCezaHukukuPage extends StatelessWidget {
  const KpssCezaHukukuPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Ceza Hukuku')),
      body: const Center(child: Text('Ceza Hukuku içeriği burada yer alacak.')),
    );
  }
}
