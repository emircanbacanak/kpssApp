import 'package:flutter/material.dart';

class KpssMedeniHukukPage extends StatelessWidget {
  const KpssMedeniHukukPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Medeni Hukuk')),
      body:
          const Center(child: Text('Medeni Hukuk içeriği burada yer alacak.')),
    );
  }
}
